#############################################
# Ejemplo de variables para el BOT          #
#############################################

# URL interna del backend (en docker-compose usar http://api:3000)
BACKEND_URL=http://localhost:3000

# Token JWT para acceder al listado de usuarios (obtenelo con /api/auth/login)
BOT_BACKEND_TOKEN=PEGA_ACA_EL_ACCESS_TOKEN

# Puerto HTTP (el Dockerfile ya expone 5001 por defecto)
BOT_HTTP_PORT=5001

# (Opcional) Token interno que debe enviar el frontend en header x-internal-token
# BOT_API_TOKEN=token-compartido-seguro

# (Opcional) Lista fija de correos para pruebas si NO querés pegarte al backend
# TEST_EMAILS=demo1@test.com,demo2@test.com

# Mailgun (si no se setean queda en modo simulación)
# MAILGUN_API_KEY=key-xxxx
# MAILGUN_DOMAIN=mg.tudominio.com
# MAIL_FROM="SIAADE <no-reply@tudominio.com>"

# Nivel de log
LOG_LEVEL=info
# === Backend ===
BACKEND_URL=http://localhost:3000
BOT_BACKEND_TOKEN=coloca_un_jwt_valido

# === Mailgun ===
MAILGUN_API_KEY=key-xxxxxxxxxxxxxxxxxxxxxxx
MAILGUN_DOMAIN=tu-dominio.mailgun.org
MAIL_FROM=No Reply <no-reply@tu-dominio.mailgun.org>

# === Log ===
LOG_LEVEL=info
