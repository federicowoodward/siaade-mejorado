# === Runtime ===
NODE_ENV=production
PORT=3000

# === DB (usa el nombre del servicio de docker-compose) ===
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=Joeldatabase*
DB_DATABASE=dbsiaade2
DB_SSL=false
# Alternativa (si preferís URL única en vez de los campos de arriba):
# DATABASE_URL=postgresql://postgres:Joeldatabase*@postgres:5432/dbsiaade2

# === JWT ===
JWT_SECRET=poné-uno-bien-largo-y-aleatorio
JWT_EXPIRES_IN=1h
USE_BCRYPT=true

# === Logger TypeORM a archivo ===
ENABLE_FILE_LOGGER=true
LOG_DIR=/app/logs
LOG_FILE=typeorm.log.txt
MAX_LOG_BYTES=1048576
SLOW_QUERY_MS=200
