# Backend .env example (deploy separado en Railway)
# Copia a backend/.env (no subir) y rellena valores.

NODE_ENV=production
PORT=3000

# Lista separada por coma de orígenes adicionales permitidos (además de localhost)
# Ejemplo: https://mi-front.app,https://otro.com
ALLOWED_ORIGINS=

# Opción 2: Variables sueltas
DB_HOST=yamabiko.proxy.rlwy.net
DB_PORT=25526
DB_USERNAME=postgres
DB_PASSWORD=nmdgxYtGTElSHckKdjpQcPmtkJsUdKoN
DB_DATABASE=railway
DB_SSL=false

# CORS (URL frontend desplegado en Railway una vez lo tengas) - legado, preferir ALLOWED_ORIGINS
FRONTEND_ORIGIN=https://tu-front.railway.app

# Para entorno local típico:
# NODE_ENV=dev
# DB_HOST=localhost
# DB_PORT=5432
# DB_USERNAME=postgres
# DB_PASSWORD=postgres
# DB_DATABASE=siaade
# DB_SSL=false

# JWT
JWT_SECRET=cambia_este_secreto_super_largo

# Logs (opcional)
LOG_DIR=./logs
LOG_FILE=typeorm.log.txt
MAX_LOG_BYTES=1048576   # 1MB (para futuro, poné 20971520 para 20MB)
SLOW_QUERY_MS=200