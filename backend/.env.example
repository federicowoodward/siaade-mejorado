# Backend .env example (deploy separado en Railway)
# Copia a backend/.env (no subir) y rellena valores.

NODE_ENV=production
PORT=3000

# Usa SOLO una opción: DATABASE_URL o variables sueltas
# Opción 1: URL completa Railway (editar <PASSWORD>)
# DATABASE_URL=postgresql://postgres:<PASSWORD>@yamabiko.proxy.rlwy.net:25526/railway

# Opción 2: Variables sueltas
DB_HOST=yamabiko.proxy.rlwy.net
DB_PORT=25526
DB_USERNAME=postgres
DB_PASSWORD=nmdgxYtGTElSHckKdjpQcPmtkJsUdKoN
DB_DATABASE=railway
DB_SSL=false

# CORS (URL frontend desplegado en Railway una vez lo tengas)
FRONTEND_ORIGIN=https://tu-front.railway.app

# JWT
JWT_SECRET=cambia_este_secreto_super_largo

# Logs (opcional)
# LOG_LEVEL=debug
