## Backend
PORT=3000
DATABASE_URL=
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=siaade
DB_SSL=false
JWT_SECRET=cambia_esto
NODE_ENV=production
FRONTEND_ORIGIN=http://localhost:4000

## Frontend (server estático)
SPA_PORT=4000

## Notas
# Copia este archivo a .env y ajusta los valores para producción (Railway inyectará sus propios valores de DB).
# En Railway define variables seguras (no subas el archivo .env real al repo).